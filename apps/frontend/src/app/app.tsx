import { Routes } from '@group1/frontend/ui-shared';
import { RecoilRoot } from 'recoil';

export const App = () => {
  return (
    <RecoilRoot>
      <Routes />;
    </RecoilRoot>
  );
};

export default App;
